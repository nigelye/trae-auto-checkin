# TRAE 每日自动签到

自动完成 TRAE 每日签到，领取积分（免费用户每天150积分，会员用户每天200积分）。

## 文件说明

| 文件 | 说明 |
|------|------|
| `trae_checkin.py` | 主签到脚本（Python） |
| `checkin_config.json` | 配置文件，存放 access_token |
| `run_checkin.bat` | 手动运行签到（有窗口） |
| `run_checkin_hidden.vbs` | 隐藏运行签到（用于定时任务） |
| `setup_task.ps1` | 设置Windows定时任务（需管理员权限） |
| `checkin.log` | 签到日志（自动生成） |

## 快速开始

### 第一步：获取 access_token

1. 打开浏览器，访问 https://www.trae.cn/work 并登录
2. 按 `F12` 打开开发者工具
3. 切换到 **Network（网络）** 标签
4. 在 TRAE 页面点击头像 → 签到
5. 在开发者工具中找到 `checkin_credits` 相关的请求
6. 点击该请求，在 **Headers（请求头）** 中找到 `Authorization`
7. 复制 `Bearer ` 后面的一长串字符（这就是 access_token）

### 第二步：配置 token

1. 打开 `checkin_config.json` 文件
2. 将 `"在这里填入你的access_token"` 替换为你刚才复制的 token
3. 保存文件

### 第三步：测试运行

双击 `run_checkin.bat`，看看是否能成功签到。

如果成功，你会看到类似这样的输出：
```
[2026-09-15 10:30:00] ==================================================
[2026-09-15 10:30:00] 开始执行 TRAE 每日签到任务
[2026-09-15 10:30:00] 正在检查签到状态...
[2026-09-15 10:30:01] 今日未签到，正在领取积分...
[2026-09-15 10:30:02] 签到成功！获得 200 积分
[2026-09-15 10:30:02] 签到任务结束
[2026-09-15 10:30:02] ==================================================
```

### 第四步：设置定时任务

1. 右键点击 `setup_task.ps1`，选择 **"使用 PowerShell 运行"**
   - 或者：以管理员身份打开 PowerShell，进入脚本目录，执行 `.\setup_task.ps1`
2. 按照提示操作即可
3. 任务会在每天早上 9:00 自动运行

## 常见问题

### Q: token 会过期吗？
A: 会的，通常有效期为数天到数周。如果签到失败，重新获取一次 token 即可。

### Q: 如何查看签到记录？
A: 查看 `checkin.log` 文件，里面有每次签到的详细日志。

### Q: 如何修改签到时间？
A: 
1. 按下 `Win + R`，输入 `taskschd.msc` 回车
2. 找到 "TRAE每日签到" 任务
3. 右键 → 属性 → 触发器 → 编辑
4. 修改开始时间为你想要的时间

### Q: 如何取消定时任务？
A:
1. 按下 `Win + R`，输入 `taskschd.msc` 回车
2. 找到 "TRAE每日签到" 任务
3. 右键 → 删除

### Q: 签到失败怎么办？
A:
1. 检查网络连接是否正常
2. 检查 `checkin.log` 中的错误信息
3. 如果提示 token 无效，重新获取 token 并更新配置文件
4. 确保你已经注册并登录了 TRAE 账号

## 安全说明

- 你的 `access_token` 只保存在本地，不会上传到任何服务器
- 请不要将你的 token 分享给他人
- 脚本只调用官方的签到 API，不会进行任何其他操作

## 技术细节

- 签到状态检查 API: `POST https://api.trae.cn/trae/api/v2/ug/checkin_credits/status`
- 领取积分 API: `POST https://api.trae.cn/trae/api/v2/ug/checkin_credits/claim`
- 免费用户每日签到获得 150 通用积分
- 会员用户每日签到获得 200 通用积分
- 积分有效期 31 天
